const API_URL_BASE = 'https://new-stellarburgers.education-services.ru/api';

export const API_URL_INGREDIENTS = `${API_URL_BASE}/ingredients`;

export const API_URL_ORDERS = `${API_URL_BASE}/orders`;

export const API_URL_REGISTER = `${API_URL_BASE}/auth/register`;

export const API_URL_LOGIN = `${API_URL_BASE}/auth/login`;

export const API_URL_REFRESH_TOKEN = `${API_URL_BASE}/auth/token`;

export const API_URL_LOGOUT = `${API_URL_BASE}/auth/logout`;

export const API_URL_USER_DATA = `${API_URL_BASE}/auth/user`;

export const API_URL_USER_PASSWORD_FORGOT = `${API_URL_BASE}/password-reset`;

export const API_URL_USER_PASSWORD_RESET = `${API_URL_BASE}/password-reset/reset`;
